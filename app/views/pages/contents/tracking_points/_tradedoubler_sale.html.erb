<% tradedoubler_params = {
    'organization' => '1167498',
    'event' => tracking_point.event,
    'orderNumber' => registration.public_id,
    'reportInfo' => '',
    'tduid' => session[:tduid],
    'checksum' => Digest::MD5.hexdigest("xxxx#{ registration.public_id }#{registration.total_sum}"),
    'orderValue' => registration.total_sum.awesome_format,
    'currency' => 'SEK' } 
%>
<img src="https://tbs.tradedoubler.com/report?<%= tradedoubler_params.collect {|key, value| "#{key}=#{CGI.escape(value)}" unless value.nil?}.join('&') %>" alt="" style="border: none;" width="1" height="1">